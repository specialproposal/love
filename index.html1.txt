<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Proposed Her</title>
    
    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #fbc4cb; /* Romantic pastel pink background */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow: hidden; /* Prevents scrollbars when button flees */
        }

        .container {
            text-align: center;
            background: #fbc4cb; 
            padding: 20px;
            max-width: 450px;
            width: 90%;
        }

        .gif-box {
            max-width: 200px;
            margin: 0 auto;
            border-radius: 10px;
            overflow: hidden;
        }

        h1 {
            color: #1a1a1a;
            font-size: 24px;
            margin-top: 20px;
            font-weight: bold;
        }

        p {
            color: #4a4a4a;
            font-size: 14px;
            margin-top: -5px;
        }

        .btn-wrapper {
            margin-top: 20px;
            display: flex;
            justify-content: center;
            gap: 15px;
        }

        /* Pill-shaped clean layout matching screenshots */
        .btn {
            padding: 10px 25px;
            font-size: 14px;
            font-weight: bold;
            border: none;
            border-radius: 20px;
            cursor: pointer;
            background-color: white;
            color: black;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            transition: transform 0.1s ease;
        }

        .btn:active {
            transform: scale(0.95);
        }

        .no-btn {
            z-index: 9999; /* Ensures button stays on top when moving */
        }
    </style>
</head>
<body>

    <div class="container">
        <div id="gif-container" class="gif-box">
            <div class="tenor-gif-embed" data-postid="22885016" data-share-method="host" data-aspect-ratio="1" data-width="100%"></div>
        </div>

        <h1 id="question-text">Do you love me?</h1>
        <p id="sub-text">nvn is all yours</p>

        <div class="btn-wrapper" id="action-buttons">
            <button class="btn yes-btn" id="yes-btn">Yes</button>
            <button class="btn no-btn" id="no-btn">No</button>
        </div>
    </div>

    <script type="text/javascript" async src="https://tenor.com/embed.js"></script>

    <script>
        const noBtn = document.getElementById('no-btn');
        const yesBtn = document.getElementById('yes-btn');
        const questionText = document.getElementById('question-text');
        const subText = document.getElementById('sub-text');
        const gifContainer = document.getElementById('gif-container');
        const actionButtons = document.getElementById('action-buttons');

        let noCount = 0;

        // Data Storage Array containing properties for Step 2, 3 and 4 
        const steps = [
            {
                text: "Please think again! 🥺",
                sub: "itni jaldi na matt bolo✨",
                postId: "15152233630689973273" 
            },
            {
                text: "Ek aur baar Soch lo! 🥺",
                sub: "kyu aisa kar rahi ho Pls Maan jao 🥺",
                postId: "16335198" 
            },
            {
                text: "beautiful pls Man jaao na! Kitna code likh waogi 😭",
                sub: "bhut glt baat hai yrr 😭",
                postId: "21721544" 
            }
        ];

        // Refreshes the Tenor script parsing to load updated inline embeds instantly
        function reloadTenor() {
            const oldScript = document.querySelector('script[src="https://tenor.com/embed.js"]');
            if (oldScript) oldScript.remove();
            const newScript = document.createElement('script');
            newScript.src = "https://tenor.com/embed.js";
            newScript.async = true;
            document.body.appendChild(newScript);
        }

        // Random coordinates calculation engine for runaway action
        function moveButton() {
            const x = Math.random() * (window.innerWidth - noBtn.offsetWidth);
            const y = Math.random() * (window.innerHeight - noBtn.offsetHeight);
            noBtn.style.position = 'fixed';
            noBtn.style.left = `${x}px`;
            noBtn.style.top = `${y}px`;
        }

        // Step Progression Handler Trigger
        noBtn.addEventListener('click', (e) => {
            if (noCount >= 3) {
                e.preventDefault();
                moveButton();
                return;
            }

            const currentStep = steps[noCount];
            questionText.innerText = currentStep.text;
            subText.innerText = currentStep.sub;
            
            // Dynamic generation of newer step embeds
            gifContainer.innerHTML = `<div class="tenor-gif-embed" data-postid="${currentStep.postId}" data-share-method="host" data-aspect-ratio="1" data-width="100%"></div>`;
            reloadTenor();

            noCount++;

            // Activates runaway parameters immediately when entry point hits Step 4
            if (noCount >= 3) {
                noBtn.addEventListener('mouseover', moveButton);
                noBtn.addEventListener('touchstart', (e) => {
                    e.preventDefault();
                    moveButton();
                });
            }
        });

        // Final Success Step Trigger (Step 5)
        yesBtn.addEventListener('click', () => {
            questionText.innerText = "I knew it! You Love me a lot 😘";
            subText.innerText = "Forever & Always ❤️";
            gifContainer.innerHTML = `<div class="tenor-gif-embed" data-postid="22049195" data-share-method="host" data-aspect-ratio="1" data-width="100%"></div>`;
            reloadTenor();
            
            // Clean up screen view UI blocks
            actionButtons.style.display = 'none';
        });
    </script>
</body>
</html>
